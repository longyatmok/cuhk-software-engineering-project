{% macro showMessage(title, subtitle ,bar_text) %}
<div class="body box home">
	<div class="content">
		<div class="header">
			<div class="content">
				<h1>{{title|default('找不到網頁')}}</h1>
				<div class="subtitle">
					{{subtitle|default('你正在檢視的頁面的內容還沒有設定，未能顯示。')}} 
				</div>
			</div>
			<div class="bottom_bar">
				{{bar_text|default('')}} 
			</div>
		</div>
	</div>
</div>
{% endmacro %}


<div class="body box home">
	<div class="content">
		<div class="header">
			<div class="content">
				<h1>{% block body_header %}{{title|default('找不到網頁')}}{% endblock body_header %}</h1>
				<div class="subtitle">
					{% block body_content %}{{subtitle|default('你正在檢視的頁面的內容還沒有設定，未能顯示。')}}{% endblock body_content %}
				</div>
			</div>
			<div class="bottom_bar">
				{{bar_text|default('')}} 
			</div>
		</div>
	</div>
</div>